<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.makeus.urirang.android">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".src.ApplicationClass"
        android:allowBackup="false"
        android:fullBackupContent="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="AllowBackup">

        <activity android:name=".src.splash.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Login -->
        <activity
            android:name=".src.login.social.SocialLoginActivity"
            android:screenOrientation="portrait" />
        <activity
            android:name=".src.login.email.EmailLoginActivity"
            android:screenOrientation="portrait" />


        <!-- Main -->
        <activity
            android:name=".src.main.MainActivity"
            android:screenOrientation="portrait" />
        <activity android:name=".src.login.find.FindPasswordCompleteActivity" />
        <activity android:name=".src.login.find.FindPasswordActivity" />
        <activity android:name=".src.login.info.InfoActivity" />
        <activity android:name=".src.login.signup.SignUpActivity" />

        <!-- Kakao social login-->
        <meta-data
            android:name="com.kakao.sdk.AppKey"
            android:value="@string/kakao_app_key" />

        <!-- Firebase Cloud Messaging Service -->
        <service
            android:name=".src.fcm.FirebaseCloudMessagingService"
            android:enabled="true"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>


    </application>

</manifest>